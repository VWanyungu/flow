This app:
1. Gets playlist info from spotify using a playlost url
2. Makes request to optimize server and receives the optimum order if songs for smooth transitions
3. Creates a new playlist with the new order
